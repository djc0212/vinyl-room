import { CollectionView } from "@/components/CollectionView";

export const metadata = {
  title: "The Collection | Vinyl Room",
};

export default function CollectionPage() {
  return <CollectionView />;
}
